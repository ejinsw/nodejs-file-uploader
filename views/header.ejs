<header class="flex items-center w-full h-20 border-b px-12 bg-white">
  <h1 class="text-2xl font-bold"><a href="/">File Uploader</a></h1>
  <div class="flex items-center ml-auto gap-4">
    <% if (!user) { %>
    <a href="/login">Login</a>
    <a href="/register">Register</a>
    <% } else { %>
    <a href="/sign-out">Sign Out</a>
    <% } %>
  </div>
</header>